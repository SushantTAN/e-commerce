"use client"
const useCurrency = () => {

  // In the future, you can fetch the currency from an API here
  // and update the state using setCurrency.

  return { currency: "Rs " };
};

export default useCurrency;